# ==================== VARIABLES D'ENVIRONNEMENT ====================
# Copier ce fichier vers .env.local et remplir les valeurs

# ==================== MONGODB (OBLIGATOIRE) ====================
MONGODB_USERNAME=votre-username
MONGODB_PASSWORD=votre-password
MONGODB_HOST=cluster0.xxxxx.mongodb.net
MONGODB_DATABASE_NAME=votre-database
MONGODB_PARAMS=retryWrites=true&w=majority
MONGODB_APP_NAME=VotreApp

# ==================== BETTER AUTH (OBLIGATOIRE) ====================
# Générer une clé secrète (32+ caractères):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET=generer-une-cle-secrete-forte

# URL de l'application
# Local: http://localhost:3000
# Production: https://votre-app.vercel.app
BETTER_AUTH_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
